{
  "name": "prerender-docker-tests",
  "version": "1.0.0",
  "description": "Tests for Prerender Docker service",
  "scripts": {
    "test": "mocha test/**/*.test.js --timeout 10000",
    "test:unit": "mocha test/unit/**/*.test.js",
    "test:integration": "mocha test/integration/**/*.test.js --timeout 30000",
    "test:load": "mocha test/load/**/*.test.js --timeout 60000",
    "test:watch": "mocha test/**/*.test.js --watch"
  },
  "devDependencies": {
    "mocha": "^10.2.0",
    "chai": "^4.3.10",
    "axios": "^1.6.2",
    "ioredis": "^5.3.2",
    "autocannon": "^7.14.0",
    "sinon": "^17.0.1"
  }
}
